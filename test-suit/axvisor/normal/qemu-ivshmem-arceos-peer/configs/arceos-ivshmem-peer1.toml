[base]
id = 2
name = "arceos-ivshmem-peer1"
guest_type = "virtualized"
cpu_num = 1
phys_cpu_ids = [2]
phys_cpu_sets = [2]

[kernel]
# The arceos smoke reads the cross-peer handshake switch from the
# generated FDT /chosen/bootargs, mirroring the Linux case marker style.
cmdline = "axvisor.pci_case=ivshmem-cross-peer"
entry_point = 0x8020_0000
image_location = "memory"
kernel_path = "${workspace}/target/aarch64-unknown-linux-musl/release/arceos-ivshmem-pci.bin"
kernel_load_addr = 0x8020_0000
dtb_load_addr = 0x8000_0000
memory_regions = [
  [0x8000_0000, 0x2000_0000, 0x7, 0],
]

[devices]
passthrough = []
disabled = [
  { path = "/pcie@10000000" },
]

[[devices.virtual]]
id = "ivshmem0"
model = "ivshmem-pci"
link_id = 1
peer_id = 1
max_peers = 2
common_size = 0
output_size = 0x7000
